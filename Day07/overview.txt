overview : ngôn ngữ lập trình javascrip
1.  cú pháp
    - nhúng trong html
        + thuộc tính sự kiện của thẻ:
            onclick, onChange
        + Thẻ script
        
        + file:js
2. các khái niệm cơ bản trong nn lập trình
    - biến, hằng, kiểu dữ liệu, toán tử, toán hạng, phép toán, biểu thức, câu lệnh, khối lệnh,...
3. in/ out

4. các cấu trúc điều khiển (3 loại)
4.1: Lựa chọn/ rẽ nhánh
4.1.1: if (4 dạng)
    a) if đơn
        if (expression-condition){
            statements;
        }
    b) if đầy đủ
        if (expression-condition){
            statements - true;
        }
        else{
            statements - false
        }
    c) if bậc thang :


    d) if nested
        if(expr1){
            statements;
            if(espr2){...}
        }else{

        }
4.1.2: switch case
    switch(expression){
        case value1:
            statements;
            break;
        case value2:
            statements;
            break;
            ....
        [default:
            statements;
            break;
        ]
    }
4.2 :Lopp (lặp)
4.2.1: lặp for( 4 dạng)
    a) for đầy đủ:
        for(initial; condition; step){
            statement block ;
        }
    b) for khuyết:
    *)  for(; condition; step){
            statement block ;
        }
    *)  for(initial; ; step){
            statement block ;
        }
    *)  for(initial; condition; ){
            statement block ;
        }
    c) for khuyết toàn bộ
        for(; ;){
            statement block;
        }
    d) for nobody
        for(initial, statement; condition; step, statement){
           ;
        }
4.2.2 lặp while
    while(condition){
        statement block;
    }
4.2.3 lặp do while
    do{
        statement block;
    }while(condition);

4.2.4 for collection
    foreach(..)
    for(...)
    forEach(...)
    ....
4.3 Lệnh nhảy
    break;
    continue:
    return;
    ....

5. khái niệm về mảng
    - mảng 1 chiều
    - mảng đa chiều

6. khái niệm xử lý xâu (chuỗi)

7.hàm => các đơn vị xử lý

8. khái niệm về lập trình hướng đt trong js

9. các tính năng nâng cao